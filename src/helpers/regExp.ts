export const ruNameRegExp = /^[А-Яа-яЁё ]+$/;
export const enNameRegExp = /^[A-Za-z ]+$/;
export const scoreRegExp = /^\d\d/;
export const levelRegExp = /^(A1)|(A2)|(B1)|(B2)|(C1)|(C2)/;
export const rankingRegExp = /\(((Beginner)|(Elementary)|(Pre-Intermediate)|(Intermediate)|(Upper-Intermediate)|(Advanced))\)/;
